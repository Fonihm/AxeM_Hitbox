# AxeM_Hitbox v1.1

## 🔹 Что делает модуль
Модуль позволяет создавать и управлять **хитбоксами** в Roblox.  
Он подходит для боевых механик, интерактивных объектов и любых ситуаций, где нужно отслеживать столкновения с моделями.  

Особенности:
- Поддержка различных режимов касания: `"single"`, `"cooldown"`, `"always"`
- Визуализация хитбокса с настраиваемым цветом и прозрачностью
- Простое управление через runtime-сеттеры
- Игнорирование выбранных объектов при проверке

---

## 🔹 Параметры конструктора

| Параметр | Тип | Описание |
|-----------|-----|----------|
| `AnchorPart` | BasePart | Обязательный. Объект, к которому привязан хитбокс |
| `Size` | Vector3 | Размер хитбокса |
| `Offset` | Vector3 | Смещение хитбокса относительно AnchorPart |
| `Color` | Color3 | Цвет визуала |
| `Visible` | boolean | Показывать визуал |
| `VisibleTransparency` | number | Прозрачность визуала |
| `Anchored` | boolean | Зафиксированность визуала |
| `CanCollide` | boolean | Возможность столкновения визуала с другими объектами |
| `TouchMode` | string | Режим касания: `"single"`, `"cooldown"`, `"always"` |
| `TouchCooldown` | number | Задержка между касаниями (сек) |
| `OnModelTouched` | function | Функция вызывается при касании модели |
| `IgnoreList` | {Instance} | Список объектов, которые нужно игнорировать |
| `PollRate` | number | Частота проверки столкновений |
| `AutoParent` | Instance | Родитель визуала, по умолчанию workspace |
| `Debug` | boolean | Включение режима тестирования |

---

## 🔹 Runtime-сеттеры

| Метод | Описание |
|-------|----------|
| `SetSize(Vector3)` | Изменяет размер хитбокса |
| `SetColor(Color3)` | Изменяет цвет визуала |
| `SetVisible(boolean, optionalTransparency)` | Показывает/скрывает визуал |
| `SetOffset(Vector3)` | Изменяет смещение хитбокса |
| `AddIgnore(Instance)` | Добавляет объект в игнор-лист |
| `RemoveIgnore(Instance)` | Убирает объект из игнор-листа |
| `SetTouchMode(string)` | Устанавливает режим касания |
| `SetTouchCooldown(number)` | Устанавливает задержку между касаниями |
| `SetOnModelTouched(function)` | Устанавливает функцию колбэка при касании |
| `ResetTouches(optionalModel)` | Сбрасывает историю касаний для модели или всех |
| `HasModelTouched(model)` | Проверяет, касалась ли модель хитбокса |
| `Enable()` | Включает хитбокс |
| `Disable()` | Выключает хитбокс |
| `Destroy()` | Уничтожает хитбокс и очищает память |

---

## 🔹 Примеры использования

```lua
local AxeM_Hitbox = require(game.ReplicatedStorage.AxeM_Hitbox)

-- Создание хитбокса
local hitbox = AxeM_Hitbox.new({
    AnchorPart = script.Parent,
    Size = Vector3.new(4, 4, 4),
    Visible = true,
    TouchMode = "cooldown",
    TouchCooldown = 1,
    OnModelTouched = function(model, hb, part)
        print(model.Name .. " потрогал хитбокс!")
    end
})

-- Включение хитбокса
hitbox:Enable()

-- Изменение цвета и размера во время работы
hitbox:SetColor(Color3.fromRGB(0,255,0))
hitbox:SetSize(Vector3.new(6,6,6))
